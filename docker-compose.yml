version: '3.8'

services:
    traefik:
        image: traefik:v2.10
        container_name: traefik
        restart: unless-stopped
        security_opt:
            - no-new-privileges:true
        networks:
            - "docker_lan_network"
        ports:
            - "80:80"
            - "443:443"
        environment:
            - TZ=Europe/Berlin
            - TRAEFIK_LOG_LEVEL=${TRAEFIK_LOG_LEVEL:-INFO}
            - TRAEFIK_LOG_FORMAT=json
        volumes:
            - /var/run/docker.sock:/var/run/docker.sock:ro
            - ./traefik/traefik.yml:/traefik.yml:ro
            - ./traefik/config:/config:ro
            - ./traefik/acme.json:/acme.json:ro
        labels:
            - "traefik.enable=true"
            - "traefik.http.routers.traefik.rule=Host(`traefik.${DOMAIN}`)"
            - "traefik.http.routers.traefik.entrypoints=web"
            - "traefik.http.routers.traefik.service=api@internal"
            - "traefik.http.routers.traefik.middlewares=traefik-auth"
            - "traefik.http.middlewares.traefik-auth.basicauth.users=${TRAEFIK_DASHBOARD_USER}:$$apr1$$395SF7/r$$BvA3aWpIyQ0RSdFXRuRUE1"
            # Read-only API for service status checks (GET /api/http/services/*)
            - "traefik.http.routers.traefik-api-readonly.rule=Host(`traefik.${DOMAIN}`) && PathPrefix(`/api/http/services`) && Method(`GET`)"
            - "traefik.http.routers.traefik-api-readonly.entrypoints=web"
            - "traefik.http.routers.traefik-api-readonly.priority=200"
            - "traefik.http.routers.traefik-api-readonly.service=api@internal"
            - "traefik.http.routers.traefik-api-readonly.middlewares=traefik-api-readonly-auth"
            - "traefik.http.middlewares.traefik-api-readonly-auth.basicauth.users=${TRAEFIK_API_MONITOR_USER:-monitor-api}:$$apr1$$SbYBr96F$$FB2EfgNHxFNUp0BwnZNlK1,${TRAEFIK_DASHBOARD_USER}:$$apr1$$395SF7/r$$BvA3aWpIyQ0RSdFXRuRUE1"
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "3"

networks:
    docker_lan_network:
        external: true
